"""Local Streamlit pages package.

This file keeps the `pages` directory importable as a package so shared modules
such as `pages._ui_shared` can be loaded consistently across local runs and
Streamlit Cloud deployments.
"""
